export async function generateResumeVariant(draft: any, posting: any): Promise<string> {
  // Placeholder: In real implementation, compare posting embedding vs resume_variant embeddings
  return 'mock-resume-id';
}
